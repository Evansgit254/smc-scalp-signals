name: Autonomous ML Retraining

on:
  workflow_dispatch: # Manual only until audit/ and training/ jobs are restored

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Retraining disabled
        run: |
          echo "Autonomous retraining is disabled until the training and audit pipeline is rebuilt."
