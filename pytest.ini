[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
norecursedirs = research
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
markers =
    authentic: tests that require live production data or infrastructure (Source of Truth)
